#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    /* code */
        int x, y;
        int hsh = 1;
        hsh = hsh + 1;
    
    while(hsh < 5)
        for(x > 0; x <= 5; x++) {
            
                printf("$");
        }

    printf("Stop programm!\n");
    return 0;
}

#include <stdio.h>
#include <string.h>

int main(void) {
	char word[40];
	printf("Infect\n ", word);
	size_t intsize;
	intsize = sizeof(word);
	printf("The symblos has a %lu parameter\n ", intsize);
	return 0;
	}
